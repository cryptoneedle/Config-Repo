spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: wf1994429
    url: jdbc:mysql://localhost:3306/springcloud_sell?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
  jpa:
    show-sql: true #方便调试

server:
  port: 8764

env:
  test1993

author:
  name: ruoli
  birthYear: 1993

# wx-restaurant-productor: #user 自定义负载均衡策略
  # ribbon:
    # NIWSServerListClassName: com.netflix.loadbalancer.ConfigurationBasedServerList
    # NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule #修改负载均衡策略
